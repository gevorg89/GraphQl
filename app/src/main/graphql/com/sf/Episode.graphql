query EpisodeQuery($episodeId: ID!) {
    episode(id: $episodeId) {
        air_date
        characters {
            image
            name
        }
        created
        episode
        id
        name
    }
}